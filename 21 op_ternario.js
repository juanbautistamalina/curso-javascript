/* Usa el operador ternario para verificar si un número es positivo o negativo. 
Si el número es positivo, el resultado debe mostrar 'Es positivo'. 
Si el número es negativo, el resultado debe mostrar 'Es negativo'.*/

let numero = 3;
let resultado = numero >= 0 ? "Es positivo" : "Es negativo";
console.log(resultado);