/*Crea la función 'postear()' con el parámetro 'texto' que llame a otras 3 funciones internas: 
validar(texto), guardar(texto), y enviar(texto). 
No tienes que implementar estas otras 3 funciones, solo llamarlas asumiendo que ya han sido implementadas. 
Finalmente llama a esta función con el texto 'Hoy aprendí a usar funciones */

function postear(texto){
    function validar(texto){}
    function guardar(texto){}
    function enviar(texto){}
}

postear('Hoy aprendí a usar funciones');